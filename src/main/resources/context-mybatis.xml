<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:util="http://www.springframework.org/schema/util"
    xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-4.3.xsd
        http://www.springframework.org/schema/util
        http://www.springframework.org/schema/util/spring-util-4.3.xsd
        http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd">

    <util:properties id="database" location="classpath:database.properties" />

    <bean id="dataSource" class="org.springframework.jdbc.datasource.SimpleDriverDataSource">
        <!-- property name="driverClass" value="#{database['database.driverclass']}" / -->
        
        <property name="driverClass" value="#{database['database.driverclass']}" />
        <property name="url" value="#{database['database.url']}" />
        <property name="username" value="#{database['database.username']}" />
        <property name="password" value="#{database['database.password']}" />
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="typeAliasesPackage" value="study.web.shop.vo" />
        <property name="mapperLocations" value="classpath:study/web/**/dao/*.xml" />
        
        <!-- MapperLocation 지정시 Mapper 클래스와 Xml이 서로 다른 패키지라도  상관 없음.
		MapperLocation 지정 하지 않을 경우 Mapper 클래스와 Xml이 서로 같은 패키지에 있어야 서로 인식이 가능함. -->
        <!-- <property name="mapperLocations" value="classpath:study/*.xml" /> -->
    </bean>

    <bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg index="0" ref="sqlSessionFactory" />
    </bean>
    
    <tx:annotation-driven transaction-manager="transactionManager" />

    <bean id="transactionManager"  class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

	<!-- Annotation 설정 : 태그 설정으로 자동으로 매퍼를 스캔해야함. -->
    <mybatis:scan base-package="study.web.shop"/>
</beans>
